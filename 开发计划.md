# BimeTsf 开发计划

## 1. 项目概述

### 1.1 背景
- **bime**: WPF .NET 4.8 外挂式输入法，使用键盘钩子监听，SendInput 上屏
- **SampleIME**: 微软 TSF 框架示例，C++ 实现

### 1.2 目标
将 bime 移植到 TSF 框架，采用混合架构：
- **BimeTSF**: C++ TSF COM 组件，负责键盘事件捕获
- **BimeCore**: C# WPF 应用，负责输入法核心逻辑

### 1.3 设计原则
1. 尽量保留 bime 原版代码逻辑
2. 通信协议简单可靠
3. 仅针对 Windows 平台

---

## 2. 项目结构

```
port_bime/
├── BimeCore/                    # WPF .NET 4.8 应用
│   ├── App.xaml
│   ├── App.xaml.cs
│   ├── MainWindow.xaml
│   ├── MainWindow.xaml.cs
│   ├── WinCandidate.xaml
│   ├── WinCandidate.xaml.cs
│   ├── WinConfig.xaml
│   ├── WinConfig.xaml.cs
│   ├── WinAddCi.xaml
│   ├── WinAddCi.xaml.cs
│   ├── Config.cs                 # 配置管理
│   ├── States.cs                 # 全局状态
│   ├── Candi.cs                  # 候选处理
│   ├── Win32.cs                  # Windows API (精简版)
│   ├── PipeServer.cs             # Named Pipe 服务端 (新增)
│   ├── MessageHandler.cs         # 消息处理 (新增)
│   ├── ImiHelper.cs
│   ├── AutoStart.cs
│   ├── AutoSpace.cs
│   ├── SendHistory.cs
│   ├── Themes.cs
│   ├── TxtFileEncoder.cs
│   ├── VirtualDesktopManager.cs
│   ├── bime.csproj
│   └── ...
│
├── BimeTSF/                      # C++ TSF DLL
│   ├── BimeTSF.vcxproj
│   ├── DllMain.cpp               # DLL 入口
│   ├── Server.cpp                # COM 导出
│   ├── Register.cpp              # TSF 注册
│   ├── Globals.cpp/h             # 全局变量
│   ├── BimeTSF.cpp/h             # 主类
│   ├── KeyEventSink.cpp/h        # 键盘事件
│   ├── TextEditSink.cpp/h        # 文本编辑事件
│   ├── ThreadMgrEventSink.cpp/h  # 线程管理事件
│   ├── TfTextLayoutSink.cpp/h    # 光标位置
│   ├── EditSession.cpp/h         # 编辑会话
│   ├── KeyHandler.cpp/h          # 按键处理
│   ├── PipeClient.cpp/h          # Named Pipe 客户端 (新增)
│   ├── Protocol.h                # 消息协议定义 (新增)
│   ├── json.hpp                  # nlohmann/json 单头文件
│   └── ...
│
├── Protocol/                     # 协议文档
│   └── messages.md               # JSON 消息格式说明
│
├── 开发目标.md
└── 开发计划.md
```

---

## 3. 通信协议

### 3.1 传输层
- **方式**: Named Pipe
- **管道名**: `\\.\pipe\BimeIPC`
- **方向**: BimeTSF (客户端) → BimeCore (服务端)
- **模式**: 双向通信，支持请求-响应

### 3.2 消息格式 (JSON)

#### 3.2.1 键盘事件
```json
{
  "type": "key",
  "action": "down",
  "vk": 65,
  "scan": 30,
  "shift": false,
  "ctrl": false,
  "alt": false,
  "capsLock": false
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| type | string | 固定值 "key" |
| action | string | "down" 或 "up" |
| vk | number | 虚拟键码 |
| scan | number | 扫描码 |
| shift | boolean | Shift 状态 |
| ctrl | boolean | Ctrl 状态 |
| alt | boolean | Alt 状态 |
| capsLock | boolean | CapsLock 状态 |

#### 3.2.2 光标位置
```json
{
  "type": "caret",
  "x": 100,
  "y": 200,
  "width": 2,
  "height": 20
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| type | string | 固定值 "caret" |
| x | number | 光标 X 坐标 (屏幕绝对坐标) |
| y | number | 光标 Y 坐标 (屏幕绝对坐标) |
| width | number | 光标宽度 |
| height | number | 光标高度 |

#### 3.2.3 焦点变化
```json
{
  "type": "focus",
  "hwnd": 1234567,
  "processName": "notepad.exe",
  "className": "Notepad"
}
```

#### 3.2.4 响应消息
```json
{
  "type": "response",
  "success": true,
  "handled": true
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| type | string | 固定值 "response" |
| success | boolean | 操作是否成功 |
| handled | boolean | 按键是否被处理 (用于 key 事件) |

---

## 4. Phase 1: BimeCore 改造

### 4.1 改造清单

| 序号 | 任务 | 涉及文件 | 改动说明 |
|------|------|----------|----------|
| 1.1 | 创建 PipeServer | 新建 `PipeServer.cs` | Named Pipe 服务端，监听连接，接收消息 |
| 1.2 | 创建 MessageHandler | 新建 `MessageHandler.cs` | 解析 JSON 消息，分发到对应处理函数 |
| 1.3 | 移除键盘钩子 | `MainWindow.xaml.cs` | 删除 `SetWindowsHookEx` 相关代码 |
| 1.4 | 修改按键处理入口 | `MainWindow.xaml.cs` | 从 MessageHandler 接收按键事件 |
| 1.5 | 移除 UIAutomation | `packages.config`, `Win32.cs` | 移除 Interop.UIAutomationClient 依赖 |
| 1.6 | 修改光标获取 | `States.cs` | 从 IPC 消息获取，不再主动查询 |
| 1.7 | 移除光标查询代码 | `MainWindow.xaml.cs` | 删除 GetCaretPosition 相关调用 |
| 1.8 | 修改 MMService | `MMService.cs` | 保留 IPC 命令功能，可能需要合并 |

### 4.2 PipeServer 设计

```csharp
public class PipeServer : IDisposable
{
    private const string PipeName = "BimeIPC";
    private NamedPipeServerStream _pipeServer;
    private CancellationTokenSource _cts;
    
    public event Action<string> MessageReceived;
    
    public void Start();
    public void Stop();
    public Task SendResponseAsync(string response);
}
```

### 4.3 MessageHandler 设计

```csharp
public class MessageHandler
{
    private readonly MainWindow _mainWindow;
    
    public void HandleMessage(string json);
    private void HandleKeyEvent(KeyMessage msg);
    private void HandleCaretEvent(CaretMessage msg);
    private void HandleFocusEvent(FocusMessage msg);
}
```

### 4.4 保留不变的功能
- 码表加载和管理
- 候选窗口 (WinCandidate)
- 配置窗口 (WinConfig)
- 添加词库窗口 (WinAddCi)
- 配置管理 (Config)
- SendInput 上屏逻辑
- 主题系统 (Themes)
- 自动启动 (AutoStart)
- 录制功能 (SendHistory)
- 虚拟桌面支持 (VirtualDesktopManager)

---

## 5. Phase 2: BimeTSF 开发

### 5.1 基于 SampleIME 的改造

| 序号 | 任务 | 来源 | 改动说明 |
|------|------|------|----------|
| 2.1 | 创建项目 | SampleIME | 复制并重命名为 BimeTSF |
| 2.2 | 修改 CLSID/GUID | Globals.cpp | 生成新的 GUID |
| 2.3 | 保留键盘事件 | KeyEventSink.cpp | 核心功能 |
| 2.4 | 保留光标位置 | TfTextLayoutSink.cpp | 核心功能 |
| 2.5 | 保留焦点事件 | ThreadMgrEventSink.cpp | 核心功能 |
| 2.6 | 添加 PipeClient | 新建 | Named Pipe 客户端 |
| 2.7 | 添加 JSON 支持 | json.hpp | nlohmann/json 单头文件 |
| 2.8 | 移除候选窗口 | CandidateWindow.cpp 等 | 不需要 |
| 2.9 | 移除字典引擎 | TableDictionaryEngine.cpp 等 | 不需要 |
| 2.10 | 移除 Composition | Composition.cpp 等 | 暂不使用 TSF 上屏 |
| 2.11 | 简化语言栏 | LanguageBar.cpp | 可选保留或简化 |

### 5.2 PipeClient 设计

```cpp
class PipeClient {
public:
    bool Connect();
    void Disconnect();
    bool IsConnected();
    bool SendMessage(const std::string& json);
    bool ReceiveResponse(std::string& response, int timeoutMs = 1000);

private:
    HANDLE _pipeHandle;
    const wchar_t* PipeName = L"\\\\.\\pipe\\BimeIPC";
};
```

### 5.3 需要保留的 SampleIME 文件

| 文件 | 用途 |
|------|------|
| DllMain.cpp | DLL 入口 |
| Server.cpp | COM 导出 |
| Register.cpp | TSF 注册 |
| Globals.cpp/h | 全局变量 |
| SampleIME.cpp/h → BimeTSF.cpp/h | 主类 |
| KeyEventSink.cpp/h | 键盘事件 |
| ThreadMgrEventSink.cpp/h | 焦点事件 |
| TfTextLayoutSink.cpp/h | 光标位置 |
| EditSession.cpp/h | 编辑会话 |
| Private.h | 头文件包含 |
| Define.h | 常量定义 |

### 5.4 需要删除的 SampleIME 文件

| 文件 | 原因 |
|------|------|
| CandidateWindow.cpp/h | 候选窗口由 BimeCore 负责 |
| CandidateListUIPresenter.cpp/h | 同上 |
| ShadowWindow.cpp/h | 同上 |
| ScrollBarWindow.cpp/h | 同上 |
| ButtonWindow.cpp/h | 同上 |
| TableDictionaryEngine.cpp/h | 字典由 BimeCore 负责 |
| DictionarySearch.cpp/h | 同上 |
| DictionaryParser.cpp/h | 同上 |
| Composition.cpp/h | 暂不使用 TSF 上屏 |
| StartComposition.cpp/h | 同上 |
| EndComposition.cpp/h | 同上 |
| DisplayAttribute*.cpp/h | 同上 |

---

## 6. Phase 3: 集成测试

### 6.1 测试环境
- Windows 10/11 64位
- Visual Studio 2022
- .NET Framework 4.8 SDK

### 6.2 测试步骤

1. **编译 BimeTSF**
   ```
   msbuild BimeTSF\BimeTSF.vcxproj /p:Configuration=Release /p:Platform=x64
   ```

2. **注册 BimeTSF**
   ```
   regsvr32 BimeTSF.dll
   ```

3. **编译 BimeCore**
   ```
   msbuild BimeCore\bime.csproj /p:Configuration=Release
   ```

4. **启动 BimeCore**
   - 运行 BimeCore.exe
   - 确认 PipeServer 启动成功

5. **切换输入法**
   - 在输入法设置中添加 BimeTSF
   - 切换到 BimeTSF

6. **测试项目**
   - [ ] 键盘事件能正常传递到 BimeCore
   - [ ] 光标位置能正常更新
   - [ ] 候选窗口跟随光标
   - [ ] 输入中文正常
   - [ ] 翻页功能正常
   - [ ] 上屏功能正常
   - [ ] 中英文切换正常
   - [ ] 多应用程序测试 (记事本、Word、浏览器等)

### 6.3 调试方法

1. **BimeCore 调试**
   - Visual Studio 附加进程调试
   - 查看日志输出

2. **BimeTSF 调试**
   - OutputDebugString 输出
   - 使用 DebugView 查看
   - Visual Studio 附加到目标进程

---

## 7. 风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| Named Pipe 连接不稳定 | 中 | 高 | 添加重连机制，心跳检测 |
| 光标位置不准确 | 中 | 中 | 保留原有 UIAutomation 作为备选方案 |
| TSF 注册失败 | 低 | 高 | 确保 COM 注册正确，检查权限 |
| 性能问题 | 低 | 中 | 优化消息格式，减少不必要的消息 |

---

## 8. 后续优化

### 8.1 短期 (Phase 3 后)
- [ ] 优化启动流程，确保 BimeCore 先于 BimeTSF 启动
- [ ] 添加 BimeCore 自启动时自动注册 BimeTSF
- [ ] 添加配置界面支持

### 8.2 中期
- [ ] 实现 TSF 原生上屏 (替代 SendInput)
- [ ] 支持 UWP 应用
- [ ] 优化性能

### 8.3 长期
- [ ] 考虑跨平台架构
- [ ] 支持更多输入方式

---

## 9. 时间估算

| 阶段 | 预计时间 |
|------|----------|
| Phase 1: BimeCore 改造 | 2-3 天 |
| Phase 2: BimeTSF 开发 | 3-4 天 |
| Phase 3: 集成测试 | 1-2 天 |
| **总计** | **6-9 天** |

---

## 10. 参考资料

- [Text Services Framework (TSF)](https://docs.microsoft.com/en-us/windows/win32/tsf/text-services-framework)
- [SampleIME Source](https://github.com/microsoft/Windows-classic-samples/tree/main/Samples/IME)
- [Named Pipes](https://docs.microsoft.com/en-us/windows/win32/ipc/named-pipes)
- [nlohmann/json](https://github.com/nlohmann/json)
