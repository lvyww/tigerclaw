# 项目名称
BimeTsf

# 项目功能概述
Bime是一个WPF .net 4.8的外挂式输入法，监听键盘信息采用的是键盘hook，而上屏汉字与词语采用的是sendinput等win32接口。

SampleIME是微软提供的TSF框架的输入法的示例程序。使用的编程语言是C++。

我现在想把Bime移植到TSF框架。之前试过直接使用C#来实现TSF框架下的输入法，但是工作量很大，实现起来很困难，移植过程中碰到了很多bug。

现在想重新移植。使Bime保持为一个独立的WPF程序运行，而另开发一个TSF输入法，用一个侦听键盘事件，以代替Bime里的键盘钩子。

上屏的逻辑，先保持原来的SendInput等接口不变。后续再考虑使用TSF的接口上屏。

# 项目架构设计
由BimeCore与BimeTSF两部分组成。

BimeTSF：一个TSF COM组件，注册成系统的输入法，使用C++实现，主要功能是通过TSF的接口，侦听键盘事件，将键盘事件以及光标位置等信息发送给BimeCore。

BimeCore：一个独立运行的WPF .net 4.8的应用程序，保留bime原版的大部分功能，仅修改键盘钩子的逻辑，以及候选窗的定位逻辑。收到BimeTsf的键盘事件，以及光标位置后，完成按键处理、字典词库运算、上屏、候选窗绘制、状态栏绘制、上屏候选字等功能。应该去掉uiautomation和iaccessible等库的引用。

# 其它要求
尽量保留bime原版的代码逻辑。
BimeCore与BimeTSF的通信协议，可以稍微考虑未来跨平台的可能。