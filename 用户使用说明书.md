# 虎爪输入法（TigerClaw）用户使用说明书

## 1. 适用范围

本说明书适用于当前发布包目录（含 `TigerClaw.Core.exe`、`TigerClaw.Overlay.exe`、`TigerClaw.Dialog.exe`、`x64/Win32 TigerClaw.dll`、`安装.bat`、`卸载.bat`）。

## 2. 安装与卸载

### 2.1 安装

1. 解压发布包到固定目录（建议不要放到临时目录）。
2. 双击运行 `安装.bat`。
3. 按提示授予管理员权限（脚本会自动弹 UAC）。
4. 安装成功后，不要删除当前目录。配置、码表、日志都在该目录中。

说明：
- 需要 `.NET Framework 4.8`，缺失时安装脚本会自动打开下载页。
- 安装脚本会将 TSF DLL 复制到 Program Files 并注册，同时写入 Core 自启动项。

### 2.2 卸载

1. 双击运行 `卸载.bat`。
2. 按提示授予管理员权限。
3. 卸载会清理注册和 Program Files 内的 DLL；原始发布目录默认保留。

## 3. 首次使用

### 3.1 状态条

- 状态条显示 `中` / `EN` / `禁`。
- 可拖动状态条位置。
- 右键状态条可打开菜单：
  - `码表文件夹`
  - `重载码表`
  - `加词`
  - `方案`
  - `主题`
  - `设置`
  - `退出`

### 3.2 常用按键（默认行为）

- `Shift`：切换中英文（可在配置中关闭）。
- `Ctrl+空格`：切换中英文（可在配置中关闭）。
- `Ctrl+=`：打开“加词”窗口（可在配置中关闭）。
- 输入编码后：
  - `Space`：上屏首选
  - `1-9,0`：选重（`0` 对应第 10 选）
  - `;`：2 选（启用“分号次选”时）
  - `'`：3 选（启用“引号三选”时）
  - `Esc`：清空当前编码
  - `Enter`：按“回车清屏”配置执行
  - `Tab`：按“TAB清屏”配置执行

高级用户功能：
- 组词中按 `Ctrl+数字`：将对应候选置顶。
- 组词中按 `Ctrl+Shift+数字`：删除对应候选（用户层调整）。

## 4. 配置说明（config.txt）

### 4.1 文件位置与格式

- 文件：`config.txt`（与 `TigerClaw.Core.exe` 同目录）。
- 编码建议：`UTF-8`。
- 每行格式：`配置项<TAB>值`。
- 注释行以 `#` 开头。

布尔值可写：`是/否`、`true/false`、`on/off`、`1/0`。

### 4.2 常用配置项

- `默认中文`：启动时默认中文状态。
- `shift切换中英文`：是否允许 Shift 切换中英文。
- `Ctrl+空格切换中英文`：是否允许 Ctrl+空格切换中英文。
- `Ctrl+等号手动加词`：是否允许 Ctrl+= 打开加词窗口。
- `回车清屏`：组词中回车时是否仅清码（不把编码字母上屏）。
- `TAB清屏`：组词中 Tab 是否清码。整句有候选时，Tab 选重优先，不会清屏。
- `翻页键`：可选 `- =`、`[ ]`、`Shift Tab/Tab`、`PageUp/PageDown`。
- `每页候选个数`：1~10。
- `最大码长`：1~16。
- `最大码长无重自动上屏`：达到最大码长后无重码是否自动上屏。
- `中英文不限长混合输入`：默认关闭。允许超过最大码长，暂存顶字上屏的候选字词，最后一起上屏。已完成但无码的编码段会显示并上屏其原始英文字符；编码进行中可用 Shift+字母保留大写，按回车（且“回车清屏”为“否”）会按原大小写上屏本轮完整英文编码。此模式下暂停“最大码长无重自动上屏”。
- `整句输入`：默认关闭。连续输入整句编码，由 Core 自动切分并生成整句候选。每个字优先采用最短的首选码；没有首选码时采用最短码。字频前 1500 的单字仍只走这条最优码；1500 名以后的单字还允许用其非最优码检索。整段编码（不计切分空格）长度不超过 4 时，每个编码可检索该码位上的全部字词，不要求必须是首选；非首选按码表候选顺序排在全部首选字词之后，不会抢到首选前面。超过 4 码后，未带选重符的编码段仍只检索该码位的第一候选；第二候选及以后必须明确选重，其中分号等同于第 2 候选、单引号等同于第 3 候选，数字指定对应候选位。一码字只允许作为完整的一码输入，长串中的每个切分至少消耗两键。码表中的词语也可以作为一条切分路径。
- `自动启用整句模式`：默认开启。不修改“整句输入”开关；当“整句输入”未开启且当前方案名称含“整句”时，整句功能仍然生效。
- `整句神经重排`：默认开启，但只在整句功能生效且存在运行时模型时生效。Core 会按需启动 `TigerClaw.Sentence.exe`，使用 Qwen3 0.6B Q8 重排三元模型的前 5 个候选；无需手动启动。进程或模型不可用时自动继续使用本地三元模型候选。
- `空码自动清屏`：无码时是否自动清理编码状态。
- `中文状态下使用英文标点`：中文模式下输出英文标点。
- `分号次选`、`引号三选`：控制对应选重键；整句模式下只有开启后，分号或单引号才会加入编码，否则按标点处理。
- `/输出顿号`：控制 `/` 键的标点行为。
- `码表存储位置`：码表根目录（相对路径或绝对路径）。
- `当前码表`：当前方案目录名。
- `主题`、`字体`、`字体大小`：界面显示相关设置。

整句模式按键：开启“分号次选”后，普通分号写入编码并明确选择第二候选；开启“引号三选”后，普通单引号写入编码并明确选择第三候选；普通数字明确选择对应候选位（`0` 为第 10 位，也支持 `10` 等多位数字）。对应设置关闭时，分号和单引号按普通标点处理。方向键切换并高亮当前整句候选。整句有候选时，`Tab` 向后、`Shift+Tab` 向前遍历整句候选，此时即使开启了“TAB清屏”也不会清码；没有候选时才按“TAB清屏”处理。默认空格上屏当前候选，回车上屏原始编码，Esc 清空。其他标点会先上屏当前整句候选，再上屏标点。显示编码会按照当前候选的切分路径插入空格，这些空格只用于显示，不会进入回车上屏的原始编码。整句模式不占用 `Ctrl+数字`，该快捷键会交还当前应用处理。

## 5. 自定义选重键

文件：`自定义选重键.txt`

格式：

```text
<n选> <键1> <键2> ...
```

示例：

```text
1选 0x31
2选 0x32 VK_TAB
4选 VK_CAPITAL VK_RSHIFT
10选 0x30
```

支持写法：
- 十进制键值（如 `49`）
- 十六进制键值（如 `0x31`）
- 键名（如 `VK_1`、`VK_TAB`）

注意：按键一旦绑定为选重键，在组词状态下会优先按选重逻辑处理。

## 6. 码表与注释规则

### 6.1 码表目录与文件

- 码表根目录由 `码表存储位置` 决定，默认是 `码表` 目录。
- 方案目录由 `当前码表` 决定。
- 会读取方案目录下的 `*.txt` 与 `*.dict.yaml`。

### 6.2 基本行格式

常见格式：

```text
词条<TAB>编码
词条<TAB>编码<TAB>频率
词条<TAB>频率<TAB>编码
```

### 6.3 `#` 注释与转义

- 行首 `#`：整行注释。
- 行内 `#`：其后的内容视为注释。
- 词条中需要字面量 `#` 时，写成 `\#`。

示例：

```text
示例\#词条	abcd
```

## 7. 日志与排障

### 7.1 常见日志

- 安装日志：`install.log`
- 卸载日志：`uninstall.log`

### 7.2 常见问题

- 安装失败提示 DLL 被占用：重启系统后重试 `安装.bat`。
- 安装后无法输入：确认已成功安装并在系统输入法列表中启用该输入法。
- 修改配置未生效：在状态条右键执行“重载码表”，或重启 `TigerClaw.Core.exe`。
- 码表词条含 `#` 不生效：请使用 `\#` 转义。
