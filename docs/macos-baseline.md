# macOS Baseline Alias

The canonical Phase 0 baseline for the current route is
[`macos-dotnet-baseline.md`](macos-dotnet-baseline.md).

This alias exists because earlier review notes used `macos-baseline.md` before
the .NET Hybrid plan fixed the document name.
