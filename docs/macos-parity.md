# macOS Parity Alias

The canonical Phase 0 parity matrix for the current route is
[`macos-dotnet-parity.md`](macos-dotnet-parity.md).

This alias exists because earlier review notes used `macos-parity.md` before
the .NET Hybrid plan fixed the document name.
