# 整句虎使用说明

整句虎把单字编码连续输入，由本地模型自动切分和排序。建议先掌握虎码单字编码，
再使用整句模式。

## Windows 实验功能：字词整句模式

开启“自动启用整句模式”，并使用名称含“智能”的码表方案，即启用字词整句模式。
名称同时含“智能”和“整句”时优先使用字词整句；只有“整句”时仍使用原来的自由切分。
需要与普通整句相同的本地语言模型。本功能暂未同步到 Rime 或 Fcitx5 Android。

- 单个空格分隔编码，连续两次空格提交当前所选整句；标点也可顶上整句并输出标点。
  分隔空格不会作为文字上屏。没有编码时的空格仍按普通空格处理。
- 连续字母仅在超出“最大码长”时自动分段。例如最大码长为 4，`abcdef gh`
  只能分为 `abcd | ef | gh`；不会尝试 `ab | cd | ef | gh` 等其它分法。
  每段必须完整匹配码表编码，未打完整的尾段不会借用更长编码的候选。
- 候选窗和输入位置的 composition 会把已确定分割的码段显示为模型当前最优路径中的字词，
  尚未闭合的尾段仍显示原码。后续模型排序可以更新这些文字；它们尚未上屏。
  尾码未完成或正在等待解码时，保留仍与原码匹配的上一次模型文字。删除分隔符会恢复编码显示，
  回删、选重和回车原码上屏始终操作真实输入；“编码伪装”也只作用于仍显示的原码。
- 每段的单字、符合条件的词条由模型竞争排序，不受“高频字仅使用最优码组句”和
  “允许单字重码组句”限制。沿用原有模型分、字数补偿等评分。
  编码不足“最大码长”时，码表非首位多字词必须显式选重才能出现；首位多字词、
  所有单字及满码多字词不受此限制。这里的首位指码表顺序，不是模型排序；空格和选重键不计码长。
- 数字 `1`—`9`、`0` 分别固定当前段为码表第 1—10 项并结束该段；
  启用“分号次选”“引号三选”时，`;`、`'` 也可固定第 2、3 项。
  满码后仍可选重；`ab2 cd` 有效，`ab23` 不会解释成第 23 项。
- 无效编码、越界选重或孤立选重键保留在输入中，双空格、标点不会误提交旧候选。
  回删逐个删除实际编码、空格或选重键；回车仍遵循原来的原码上屏/清屏设置。
- “整句自动提前上屏”仍默认关闭。开启后仅提交连续稳定、置信度足够的已闭合分段，
  同时消耗该段的空格或选重符。恰好满码但未出现后续键时不算闭合。
  至少保留 3 个编码字母，并遵循“保留最少编码数量”；分隔符不计入保留码数。
  空码不会触发其它隐式切分。
- 切换方案只迁移未上屏原码，清除旧排序偏好和已上屏上下文；离开字词整句时移除真实分隔空格。

下文未特别注明的整句切分规则指原来的普通整句模式。

## 版本

- Windows 虎爪版：n-gram 候选，可选 Qwen 重排；自动提前上屏默认关闭。
- Rime 实验版：纯 Lua n-gram，可跨平台部署；自动提前上屏默认开启，仍需按前端
  验证提交刷新和稳定性。
- Fcitx5 Android 原生版：独立 C++ 插件，不依赖 Rime；当前已能真机输入，发布和
  性能验收尚未完成。

离线准确率只用于模型比较，不代表所有真实输入场景，因此不在用户说明中固定写入
百分比。

## 输入

- 连续输入每个字的编码，中间不按空格。
- 一简字嵌入长句时至少输入两码，例如“一”输入 `fi`。
- 默认对字频前 1500 的单字只使用最短首选码；更生僻的字仍可用非主码。“整句允许全码组句白名单”里的字不受该限制，仍可打全码。将该项填 0 或空则不限制。
- “的”使用特设编码 `ue`。
- 非首选字词必须追加选重后缀：`;` 为第 2 项，`'` 为第 3 项，数字指定第 N 项。勾选“允许单字重码组句”后，未带选重的非首选单字也可组句，并与首选按语言模型竞争（例如 `gyygch` 可以出“羊羔”而不是只能“羊赤”）；单独打一个编码时仍是码表首选在前。多字词仍须选重。该选项不绕过高频最优码和白名单，显式选重仍然有效。

示例：

```text
hh2ah  → 慢慢来
b;ot   → 如果是
```

常用操作：

- `Space`：提交当前候选。
- `Enter`：提交原始编码。
- `Esc`：清空。
- `Up/Down`、`Tab/Shift+Tab`：切换整句候选。
- 数字键在整句编码中表示码表选重，不直接选择候选窗口中的整句。

显示编码中的切分空格只用于阅读，不会写入原始编码。

## 自动提前上屏

模型在连续多次解码中确认某个前缀足够稳定时，可以先提交该前缀并继续保留后面的
活动编码。Windows 版默认关闭，可在设置中启用；Rime/Fcitx5 Android 的默认值见
各自方案说明。开启“允许单字重码组句”后，概率提前上屏不会删除提交前完整候选中
已经成立的非首选单字续句；空码顶屏形成的新续写仍只隐式采用首选。

退格或手动切换候选会清除或暂停当前稳定证据。若某个前端出现重复提交、漏字或
明显闪烁，应先关闭该功能并记录输入编码和前端版本。

## 补充语料

补充语料只提升已有合法编码路径的排序，不增加码表条目，也不影响普通单字/词组
输入。每行格式：

```text
# 词条 权重
茧师 1000
新流行词
```

权重默认为 1000。

- Windows：当前整句方案目录下的 `补充语料.txt`，重载码表后生效。
- Rime：用户目录下的 `tiger_sentence.supplement.txt`，重新部署后生效。
- Fcitx5 Android：用户数据目录中的 TigerClaw 补充语料，重新激活输入法后加载。

常用单字会影响大量句子，给单字设置高权重时应谨慎。
